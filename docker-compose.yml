services:
  poe-tools:
    build: .
    container_name: poe-tools
    restart: unless-stopped
    environment:
      - POE2_NO_OPEN=1
      - HOST=0.0.0.0
      - POE_CONTACT=REDACTED-USER
    ports:
      - "17777:17777"
