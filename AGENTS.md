# poe-tools — project context

Read this first. Self-hosted Path of Exile 2 (PoE2) browser utility suite: pricing, crafting, rune/drop valuation, and an in-progress gear search. Local-first.
> [!session-end] Before ending a session: update the **Where we left off** section below — what changed, what's done vs open, the exact next step. If code changed, commit and push so AGENTS.md and the code stay in sync. This file is the next session's starting point; keep it current.


## Stack
- Zero-dependency Node.js — built-in http/fs/path/child_process only. No framework, no npm deps.
- Single server.js + standalone vanilla HTML/CSS/JS pages (index.html, character-upgrades.html, craft-optimizer.html).
- Run locally: node server.js -> http://127.0.0.1:17777.

## Deploy
- Repo: private GitHub skk-hub/poe-tools.
- Dev on Main PC at C:\Users\User\dev\poe2-craft-pricer-project (dir name differs from deployed name poe-tools).
- Flow: push from PC -> on Docker VM: git pull && docker compose up -d --build.
- Runs as container poe-tools on :17777. Access: LAN REDACTED-LAN-HOST:17777; Tailscale docker:17777.
- Monitor: Uptime Kuma HTTP check on /api/trade-status (no external calls).

## Data sources
- poe.ninja for prices (primary).
- GGG pathofexile.com/api/trade2 fallback — currently disabled (MAX_TRADE_FALLBACKS=0).

## Tools — status
- SPA shell (2026-06-14): index.html is the WHOLE app and is now SHELL-ONLY (~570 lines = `<head>` links + view markup + a tiny routing `<script>`). Every tool is a true inline `data-view` view (no iframes), each split into its own files: **`<tool>.css`** (`arbitrage.css`, `map-juicer.css`, `gear-search.css`, `rune-picker.css`, all prefix-scoped under `.toolroot-<x>`) + **`<tool>.js`** (`arbitrage.js`, `map-juicer.js`, `gear-search.js`, `rune-picker.js`, `craft-pricer.js`, `craft-pricer.css`; `home.js` powers the home currency strip; `waystone-data.js` is Map Juicer's data dep). What stays inline in index's `<style>` is home/hub CSS **plus a genuinely-shared tool-view baseline** (`input`/`button`/`textarea`/`.pagehead`/`.sub`/`.controls`/`.status`/`.tablewrap`/`table`/`.muted`) that arb/gear/map reuse via unscoped selectors — it stays in index because the cascade is theme→arb→map→gear→rune→index `<style>` (index last), and moving the baseline to theme.css would flip arb's `.tablewrap` override. Tools: Home (markup + `home.js` currency strip), Craft Pricer (LIVE — `/api/prices` poe.ninja material costs per route, `.toolroot-cp`), Rune Picker, Gear Search, Map Juicer, Arbitrage, More Tools — hash-routed under one shared header/nav. Collision-safety: tool CSS is **prefix-scoped** (`.toolroot-<x> …`, browser-portable — was `@scope`, converted for old-browser support); tool JS is a `window.__viewInit["<view>"]=function(){…}` lazy fn (function scope isolates the shared globals `esc/els/render/setStatus/fmt` AND defers network to first open). Arbitrage's colliding IDs were renamed `#status/#results/#summary`→`#ab*`. The shell's `showView()` calls `runViewInit(key)` (fires each view's init once) + a DOMContentLoaded re-run for direct-hash loads. Standalone `arbitrage-scanner.html`/`waystone-juicer.html`/`character-upgrades.html` are thin REDIRECTS to `index.html#<view>`. **To add/edit a tool:** its `<tool>.css` (prefix every selector with `.toolroot-<x>`), its `<tool>.js` (wrap in `window.__viewInit["<view>"]=function(){…}`), a `<section class="view toolroot-<x>" id="<view>" data-view="<view>">` in index, a nav link, and the `<link>`+`<script src>`. Then `node smoke-test.js`.
- Main hub: working
- Craft pricer: BEING REBUILT (blanked 2026-06-13 — see log) — `#craft-pricer` view shows a placeholder
- Rune picker: working
- Map Juicer (Waystone Juicer): working (new, 2026-06-13) — standalone waystone-juicer.html
- Arbitrage Scanner: working (new, 2026-06-13) — standalone arbitrage-scanner.html; Currency Exchange ex-spread flips
- Craft optimizer: experimental (standalone, not surfaced in the hub nav)
- Character upgrade guide: being replaced by Gear Search

## In progress — Gear Search (replaces character-upgrades.html)
Approved scope:
- Free-form gear import -> slot-aware filters -> budget -> Trade2 query preview -> first 20 listings -> full current-vs-candidate stat comparison.
- Build the shared adaptive Trade2 queue FIRST (trade-queue.js), then build Gear Search on top.
- No result cache. Persistent safety state only.
- Out of scope for v1: OAuth, guide logic, scoring, multi-slot scans.

## Conventions
- **Run `node smoke-test.js` after any UI/shell change.** 28 checks: pages/scripts parse, no @scope/iframe/token-cycle regressions, redirects, deep-link view init, 0 console errors, 0 overflow (desktop+mobile), a11y focus/announce, and the data-table styling (via mocked rows). Zero-dep static+HTTP checks always run; browser checks auto-find Playwright+Chromium (npx cache or global) and skip — never fail — if absent. Exit !=0 on failure. Proven to catch the deep-link regression (negative-control tested).
- Keep every tool dependency-free and local-first.
- ALL trade2 API calls go through the shared persistent adaptive queue. Never call trade2 directly.
- UI: premium dark dashboard. Shared design system in **theme.css** (linked first on every page) is the single source of color/type/spacing/radius/shadow/motion tokens — set tokens there, not per-page. v2 direction: NEUTRAL charcoal surfaces (near-black, ~0 chroma — never brown), gold strictly a sparse accent, real radii + soft layered shadows. Type: **Inter for all UI/headings/nav/buttons; Cinzel ONLY for the brand mark**; JetBrains Mono for data. Reusable components: `.btn`(+`-primary`/`-ghost`/`-sm`), `.badge`(+`-live`), `.card`/`.panel`, `.field`. theme.css also carries a scoped (`.appbar`/`.bar`) modern header + responsive nav that overrides per-page styles. CRITICAL: never alias a primary token to itself (e.g. `--gold:var(--gold)`) — it creates a CSS cycle that blanks the token. Pages keep only page-specific layout in their inline `<style>`; do NOT re-declare `:root` tokens.
- Primary author: Codex (on Main PC).

## Where we left off (update at end of each session)
- 2026-06-10: Implemented trade-queue.js shared persistent adaptive Trade2 queue and wired server.js Trade2 calls through it. /api/trade-status now includes queue metadata.
- 2026-06-10: Replaced character-upgrades.html with Gear Search v1: gear import, slot-aware editable filters, budget, query preview, first-20 listing fetch, and current-vs-candidate stat comparison. Added /api/gear-search/analyze, /api/gear-search/import-browser-export, and /api/gear-search/search.
- 2026-06-10: Updated index.html labels from Upgrade Finder to Gear Search. Dev server restarted and verified on http://127.0.0.1:17777.
- 2026-06-10: Added Gear Search total elemental resistance filter (`totalElementalRes`) using Trade2 pseudo stat `pseudo.pseudo_total_elemental_resistance`; chaos resistance remains a separate filter/stat.
- 2026-06-10: Audited Gear Search stat mappings across all slots. Added missing Trade2 mappings for evasion, energy shield, flat cold to attacks, projectile damage, and mana on kill. Gear Search now exposes only filterable stat keys, and preview returns `unsupportedFilters` instead of silently dropping unknown keys.
- 2026-06-10: Moved item equipment values to Trade2 `equipment_filters` instead of stat filters where appropriate: chest evasion uses `equipment_filters.filters.ev`, helmet energy shield uses `es`, and bow DPS uses `dps`. Keep explicit/stat filters only for affixes and pseudo stats.
- 2026-06-10: Added chest Deflection filter as Trade2 stat `explicit.stat_3040571529` (`#% increased Deflection Rating`). Gear Search price budget now uses Divine Orb (`trade_filters.price.option = "divine"`) instead of exalted.
- 2026-06-10: Tuned Gear Search default filters to use equipment filters first where appropriate: bow DPS, helmet energy shield, and chest evasion. Default filter serialization now preserves `key` values so equipment filters survive query preview/search.
- 2026-06-10: Changed Gear Search defaults to derive filter rows from the equipped item itself, default match mode to All filters, leave Min count blank, and remove the default Min ilvl control. Backend now no longer injects static slot fallback filters or ilvl constraints.
- 2026-06-10: Reworked Gear Search layout so Gear Import starts as a compact top rail and auto-collapses after Analyze/Load/Save, leaving only the reopen icon. Replaced the old equipped-slots panel with a Locked Loadout overview that aggregates projected stat changes across multiple locked listings and persists locks in localStorage.
- 2026-06-10: Extended Trade2 listings with slot and raw candidate stat payloads so the front end can lock multiple items, compare current-vs-candidate stats per item, and build a projected character overview from the combined locked loadout.
- 2026-06-11: Split Gear Search ring handling into distinct `Ring 1` and `Ring 2` slots. Imported browser-export text now preserves ring identity with synthetic slot labels, the slot selector exposes both rings, and locked/search comparisons can be made against either equipped ring independently.
- 2026-06-11: Replaced the default Gear Import body with a slot-by-slot import wizard for non-technical users. Each equipped slot has its own paste box, clipboard paste button, clear button, readiness state, and shared progress meter. Raw text / browser export import remains available under Advanced raw import.
- 2026-06-11: Added an Advanced raw import console helper at the top of Gear Import with an Open PoE2 Characters link (`https://pathofexile2.com/my-account/characters`), visible console snippet, and Copy Console Code button. The snippet now scans page JS state, scripts, and browser storage for item-like objects and copies a structured `poe2-console-export` JSON payload for pasting into Advanced raw import.
- 2026-06-11: Added named client-side Gear Import saves using `localStorage` (`poe2.gearSearch.importProfiles`) so different people/characters on the same browser can save, load, and delete separate import profiles without cookies or server accounts.
- 2026-06-11: Added separated flat damage filters plus pseudo-like combined flat damage controls. Bow exposes local flat totals/parts; quiver, gloves, and rings expose attack flat totals/parts. Official Trade2 has no real `pseudo.*` flat damage stats, so combined totals use a Trade2 component prefilter and local post-filtering after listings are fetched.
- 2026-06-11: Clarified the Gear Search total elemental resistance label to `Total elemental res (sum)` to distinguish the correct summed Trade2 pseudo from the misleading in-game/Trade2 count-style `# total Elemental Resistances` stat.
- 2026-06-11: Fixed combined flat damage search behavior. Trade2 still only shows the broad component `count` prefilter because it cannot express total flat min cheaply, but Gear Search now fetches up to 60 candidates when a combined flat post-filter is active and applies the real combined min/max locally. Query preview/search now warns that opened Trade pages only show the broad prefilter.
- 2026-06-11: Removed global critical chance from Gear Search glove filters again because glove crit appears to come from socket/augment behavior rather than a normal glove affix; keep glove filters focused on normal slot stats.
- Verification: `node --check server.js` passed. Restarted the local `server.js` process from this checkout, verified the empty state shows All filters with blank Min count and no Min ilvl, then imported a chest item and confirmed the default filters auto-filled with current values (`Evasion 900`, `Life 120`, `Total elemental resistance 67`, etc.) for the selected item. In Playwright, loaded `character-upgrades.html` with a cache-busting URL, confirmed Gear Import collapses after Load and leaves only the expand icon, searched Trade2 for a chest import, and locked a listing to verify the Locked Loadout overview and per-item unlock flow updated correctly.
- Verification: `node --check server.js` passed. Restarted `server.js`, loaded a two-ring sample in Playwright, confirmed the slot selector now exposes `Ring 1` and `Ring 2`, and switched between them to verify the default filters update to each ring's own stats (`Ring 1` life 50 / fire res 20 / cold res 20, `Ring 2` life 30 / fire res 15 / lightning res 35).
- Verification: `node --check server.js` passed. Restarted `server.js`, loaded `character-upgrades.html` in Playwright with a cache-busting URL, confirmed the import wizard renders all 10 paste slots, filled Ring 1/Ring 2 through the wizard, clicked Analyze Gear, confirmed the panel auto-collapsed, and verified Ring 1/Ring 2 default filters and query preview use each ring's own values.
- Verification: In Playwright, opened Advanced raw import and verified the console helper renders, the structured JSON-export snippet is populated, and the Copy Console Code button is present/clickable. Also POST-tested a representative `poe2-console-export` JSON payload against `/api/gear-search/import-browser-export` and confirmed it converted into copied item text with slot labels.
- Verification: `node --check server.js` passed. Loaded `character-upgrades.html` in Playwright with a cache-busting URL, confirmed the new Save name / Saved imports controls render, saved a test import profile, verified it appeared in `localStorage`, then deleted it and confirmed the profile store returned to `{}`.
- Verification: `node --check server.js` passed. Checked live Trade2 stats data and confirmed no official flat-damage pseudo IDs exist. Restarted `server.js`, POST-tested a ring import with fire/cold/lightning flat attack damage and confirmed separated stats plus `totalFlatElementalAttack`/`totalFlatAttack` are derived. Preview-tested `totalFlatElementalAttack` and confirmed it serializes to a component count prefilter with `compositeFilters.postValue`; live Trade2 search returned successfully.
- Verification: `node --check server.js` passed. Preview-tested a chest search with `totalElementalRes` min 50 and confirmed the query still sends `pseudo.pseudo_total_elemental_resistance`, not the count-style pseudo.
- Verification: `node --check server.js` passed. Restarted `server.js`, live-tested a ring search with `totalFlatAttack` min 30, and confirmed the API fetched 60 candidates with `compositeFilters.postValue.min = 30` for local post-filtering.
- Verification: `node --check server.js` passed. Restarted `server.js` and confirmed `/api/gear-search/analyze` no longer returns `critChance` in `slots.gloves.statKeys`.
- 2026-06-11: Implemented "weighted sum" workaround for "total flat elemental" damage searching. Since GGG's PoE2 Trade API does not support the `sum` filter type for these mods, Gear Search now uses the `weight` filter group with a weight of 1 assigned to each component mod (Fire, Cold, Lightning, etc.). This forces the official Trade site to correctly sum the values and filter by the requested magnitude.
- Verification: `node --check server.js` passed. Trade queries now use the `weight` group with `value: { weight: 1 }` per filter and the user's requested minimum as the group total.
- 2026-06-13: REVERTED the `sum`/`weight` composite workarounds. Verified live against anonymous Trade2 that both `sum` and `weight` stat groups return HTTP 400 "Query is too complex. Logging in will increase this limit." — they require a logged-in session, which this tool does not use. The previous two commits (`f3744a5` sum, `69253f9` weight) had broken every combined flat-damage search (`totalFlatElementalAttack`, `totalFlatAttack`). Restored the broad `count` prefilter (`value: { min: 1 }`, require >=1 component present) and rely on the existing local post-filter (`postValue` + `compositeStatsSatisfied`, fetch up to 60 candidates) to enforce the true combined min/max. Also removed a leftover `console.log("TRADE QUERY:")` debug line in `searchGear`.
- Verification (2026-06-13): `node --check server.js` passed. Restarted server. Live-tested `totalFlatElementalAttack` min 10 and `totalFlatAttack` min 10 on rings: both return 200, fetch 60 candidates, and all 20 returned listings satisfy the local min (e.g. flatElem 11–27.5). Confirmed the protected Rune Picker still prices from poe.ninja and a plain `life` ring search still works.
- 2026-06-13: Fixed the `listingPriceEx` name collision. Renamed the entry-based version (server.js ~2266, used by `searchGear`/`searchUpgradeSlot`) to `listingPriceFromEntry`; the original `listingPriceEx(price, currencyRates)` (~832) now serves only the optimizer's `fetchComparablePrices` (~909), restoring its comparable pricing (verified: comparableCount went from 0 to 10).
- 2026-06-13: Added per-listing "View on Trade" deep-ish link to Gear Search. Each `/api/gear-search/search` listing now carries a compact `tradeSpec` (category, exact price, top explicit stat mins). New endpoint `POST /api/gear-search/listing-link` builds a focused, price-sorted Trade2 search for that single item and returns its URL (status `any` so offline sellers still show). Front-end adds a "View on Trade" button per listing that opens the official PoE2 UI on that search. GGG has no true single-listing permalink, so this surfaces the item near the top of a tight search rather than in isolation.
  - Spec excludes aggregation-prone keys: derived totals, equipment props (ev/es/dps/deflection), and fire/cold/lightning res + str/dex/int (the parser folds "all elemental res"/"all attributes" into those, which would otherwise exclude the item from its own search). chaosRes kept. Stat filters use `min = floor(rolled)`; an exact band was tried and reverted (GGG stat normalization returned 0 matches).
- Verification (2026-06-13): `node --check server.js` passed. Live-tested listing-link for multiple rings: returns valid URLs, matches > 0 (item present by construction), including previously-zero resistance/attribute rings after the skip-key fix. Rune Picker re-confirmed. Note: heavy testing tripped the shared Trade2 IP rate limit; the queue handled it gracefully (endpoints return `limited` instead of erroring).
- 2026-06-13: Added listing freshness + seller-status indicators to Gear Search (helps spot stale listings — seller online but item already sold). `searchGear` listings now include `sellerStatus` (online/afk/dnd/offline, derived from `listing.account.online` via `sellerOnlineStatus`); `listedAt` (existing `listing.indexed`) is now rendered as a relative "x ago" badge instead of a raw ISO string. Front-end shows colored badges: seller status (green online / gold afk-dnd / muted offline) and age (green <1h fresh, red >=12h stale). Both come from data already in the fetch response — no extra Trade2 calls. Badge tooltip notes status is the seller account, not live item availability (GGG exposes no real-time in-stash flag). Did not change the deprecated `searchUpgradeSlot`/upgrade-finder path.
- Verification (2026-06-13): `node --check server.js` passed. One live ring search confirmed `sellerStatus` (e.g. afk/online) and `listedAt` populate per listing; cheap listings were 4-7 days old, demonstrating the staleness signal.
- 2026-06-13: Scrapped the Craft Pricer (Craft Price Checker) for a planned rebuild, per user. The `#craft-pricer` view body in index.html is now a "This tool is being rebuilt" placeholder; the controls (#league/#refresh/#status) and results #grid were removed. Kept the nav link + home card (slot reserved) and left the CRAFTS data + render()/refreshPrices() functions in place (now dead) for the redo. Because index.html's single script is SHARED with the protected Rune Picker, added null-guards so removing those DOM nodes can't throw at load and break the Rune Picker: `setStatus` returns if no #status, `render()` returns if no #grid, `refreshPrices()` returns if controls missing, and the #refresh listener is bound only `if(refreshBtn)`.
- Verification (2026-06-13): Extracted index.html inline script and `node --check` passed. Served page shows the placeholder; Rune Picker markup (#runeInput/#checkRunes/#runeRows) intact and `/api/rune-prices` still prices Desert Rune. Did not touch the Rune Picker or Gear Search.
- 2026-06-13: Built Map Juicer (Waystone Juicer) v1 — new standalone `waystone-juicer.html` + editable `waystone-data.js`, linked from the hub (nav + tool card in index.html). Patch 0.5 / Runes of Aldur. Page is divided by content type (Breach, Abyss, Delirium, Expedition, Ritual, General); each shows: tablet stash regex, "6-mod waystone to run" regex, "blue waystone to upgrade" regex (all copy-to-clipboard, 250-char-limit aware), and the blue->juiced crafting path. Plus a paste-an-item evaluator that reads the `Rarity:` line + mods and returns keep/run / upgrade / reroll, detecting tablet content type. Key 0.5 fact baked in: content mechanics come from Precursor Tablets (not waystone mods), so per-content regex = tablet regex; waystone reward mods are content-agnostic. All static/offline — NO Trade2, no poe.ninja. All game data lives in waystone-data.js with a patch stamp for easy per-patch updates. Did not touch server.js, Rune Picker, or Gear Search.
- Verification (2026-06-13): `node --check` passed on waystone-data.js and the page's inline script. Server serves /waystone-juicer.html (200) and /waystone-data.js (200, text/javascript); hub shows the nav link + card. Generated regex all <90 chars (well under 250). Replicated the evaluate() logic in Node against sample items: good blue / risky rare / breach tablet / white waystone all classified correctly.
- 2026-06-13: Baked MARKET-DERIVED mod values into Map Juicer. Used the Trade2 API's dedicated waystone `map_filters` (`map_packsize`, `map_iir`, `map_rare_monsters`, `map_magic_monsters`, `map_tier`; category `map.waystone`) to run a one-off Tier-16 price-floor sweep (cheapest exalted-priced listing at each stat threshold = market's marginal price for that roll). Findings: **Pack Size ≈ Monster Effectiveness ≫ Item Rarity (pays only ≥50%) ≫ Monster Rarity (~6ex flat).** Pack≥35 floors ~40ex; Rarity≥35 ~2-6ex. Also confirmed a 0.5 reality: **waystones have NO "Quantity of Items" stat** (gone; reward axes are Item Rarity, Pack Size, Monster Rarity, Monster Effectiveness, + Drop Chance sustain) and at T16 essentially every *listed* waystone is corrupted. Baked into `waystone-data.js`: new `marketWeights` block (source/date/per-stat weight+premiumAt+floors+tip, weight normalised to Pack Size=1.0), reordered `waystoneTargets` by market value with target %s, fixed `tokens` (dropped invalid `uantit`, lead Pack Size). In `waystone-juicer.html`: new "Mod value — what the market pays for" card (ranked bars), market-led run regex (`"ack s" "agic monst|onster eff|rar"`), and the paste-evaluator now computes a weighted **market value score** (Σ weight×rolled%) + best stat, so a pack-heavy map scores "premium" even with low rarity and a pure-rarity map is flagged cheap-to-make. Handles both copy formats ("38% increased Pack Size" and "Pack Size: +35%"). Still offline at runtime — weights are statically baked, re-run the sweep per patch. Mind the shared Trade2 rate limit when re-sweeping (I tripped it once mid-run; the queue recovered).
- Verification (2026-06-13): `node --check` passed on waystone-data.js + inline script. Node-tested the scoring: premium pack38/meff32 map → 79, pure-rarity 60% → 33, junk → 11, property-style paste → 77 (both paste formats parse). Server serves /waystone-juicer.html and /waystone-data.js (200). Temp probe scripts removed.
- 2026-06-13: Added an in-tool "Refresh from market" button to Map Juicer (live re-sweep of mod weights, replacing reliance on the static bake). Backend: `GET /api/waystone/market-weights` returns the file-cached weights (no trade calls); `POST /api/waystone/market-weights/refresh` runs `runWaystoneSweep` through the shared adaptive Trade2 queue (7 searches incl. baseline → `waystoneFloor` reads cheapest exalted Tier-16 listing per stat threshold; `robustWaystoneFloor` takes the 2nd-cheapest when the cheapest is <½ of it to dodge AFK/mispriced 1ex listings; weights normalised to the top stat, `premiumAt` = lowest threshold ≥3× baseline & ≥10ex). Guards: returns `{limited}` if the queue is blocked, a 10-min `{cooldown}` unless `force:true`, and NEVER overwrites a good cache on a partial/rate-limited sweep. Result cached to `.waystone-weights.json` (gitignored). Front-end: market card shows a `live`/`baked` tag + Refresh button + status line; `loadCachedWeights()` picks up server cache on load (graceful no-op when served statically); `MW()` overrides the baked `D.marketWeights` when live weights are present. Live run verified: Pack Size weight 1.0 (40ex @35%), Monster Effectiveness/Item Rarity ~0.5, Monster Rarity 0.03 — ranking matches the baked bake. Cooldown + GET paths confirmed to make zero trade calls.
- Verification (2026-06-13): `node --check server.js` + inline script parse passed. Restarted server (killed old PID on :17777). GET returns `weights:null` pre-cache then the cached object post-sweep; one forced refresh ran the full sweep (~50s, stayed under the 16/12s IP limit), wrote the cache; an unforced refresh returned `cooldown:true` with no trade calls. Page serves 200.
- 2026-06-13: Map Juicer evaluator now (a) shows an approximate VALUE for both item kinds and (b) suggests the best-fit CONTENT TYPE. Added `scalesWith` (statKey→0-1 benefit) to every content type in waystone-data.js (e.g. Ritual {monsterRarity:1.0, packSize:0.6}, Delirium {packSize:1.0, monsterEffectiveness:0.65}, Expedition/General {itemRarity:1.0,...}). New `contentFit(rolls)` normalises each rolled % by its ceiling, scores every content type, and returns a ranking; the evaluator appends "Best for <content> (<driver stat>-heavy) — socket <content> tablets" (+ a close runner-up). Waystones show "Est. floor value ≈ N ex" (from the baked %-curve, no live trade needed); tablets show an "Approx. value Strong/Decent/Light/Weak" band from roll-quality vs ceilings plus "This is a <content> tablet". Evaluator was already type-agnostic (parses pasted text only, not the active tab). Verified: rare-monster map → Breach/Ritual, dense pack/meff map → Abyss/Delirium, pure-rarity 72% → Expedition/General (~356ex), pack+mrare+rar → Breach. No server.js change (static files served fresh).
- 2026-06-13: CORRECTED the Map Juicer mod-value model to be %-AWARE (price-vs-% curve per stat) after realising the earlier flat per-stat weight was biased. The bias: comparing stats at the same ABSOLUTE % (pack≥35 vs rarity≥35) is unfair because roll CEILINGS differ — Pack Size maxes ~41%, Item Rarity rolls to ~84%. A finer sweep (floor at multiple %s per stat) flipped the headline: **Item Rarity has by far the highest ceiling and its price EXPLODES at the top — 30%→1ex, 50%→20ex, 70%→~325ex (~8× the best Pack Size roll).** Pack Size 20%→1ex, 30%→20ex, 40%→40ex (caps ~40); Monster Effectiveness 20%→1ex, 40%→37ex; Monster Rarity ~1ex even at ≥40%. So: at MID rolls Pack/Meff are worth more per % (~2ex/% vs rarity ~1ex/%), but at HIGH rolls Rarity wins massively (~15ex/% from 50→70 and keeps climbing). Reworked `marketWeights` in waystone-data.js to carry a `curve:[[pct,ex]]` + `ceiling` + `peakEx` per stat (ranked by peak: Rarity > Pack ≈ Meff > MonsterRarity), reordered `waystoneTargets`, and rewrote the evaluator: `curveEx()` interpolates/extrapolates a stat's ex floor at the rolled %, and a map's headline value ≈ its single BEST stat (floors are whole-map, not summed) — so a near-max pure-Rarity map now reads premium (~400ex) where the old flat model wrongly scored it ~33. Market card now shows each stat's curve + ceiling (log-scaled bar so 325 doesn't crush 40) instead of a single weight bar. Server `runWaystoneSweep` updated to the same shape with finer thresholds (Rarity 30/50/70, Pack 20/30/40, Meff 20/40, MRare 40 = 10 searches) and reads each listing's rolled prop to derive `ceiling`; front-end `loadCachedWeights` now ignores old-format caches (requires `stats[0].curve`). Deleted the stale `.waystone-weights.json` so baked curves show until a live Refresh repopulates.
- Verification (2026-06-13): finer live sweep ran clean (rarity≥70 → 325ex floor over 100 listings was the standout). `node --check` on server.js + data.js + inline script all pass. Node-tested curve scoring: pure-rarity 75% → ~401ex (premium), max-pack40+meff35 → 40ex, mid-pack30+rarity40 → 20ex (pack wins mid), mid-rarity45 → 15ex, junk → 1ex. Server restarted; GET weights → null (baked curves show); page 200.
- 2026-06-13: Added a live div↔ex readout to Map Juicer from GGG's OFFICIAL Currency Exchange (not poe.ninja). Endpoint `GET /api/waystone/exchange` → `getWaystoneExchange` POSTs the Trade2 bulk exchange (`/api/trade2/exchange/poe2/{league}`, `have:[exalted] want:[divine] engine:new`) through the shared queue, reads `bestExchangeRate` (cheapest exalted-per-divine offer with stock≥5 so a tiny-stock outlier can't skew it), and caches to `.waystone-exchange.json` (gitignored) with a 5-min TTL so a page load triggers ≤1 trade call per window; falls back to the last cached value (`stale:true`) or `{limited}` when the queue is blocked. Front-end: small `#fxRate` chip in the pagehead ("1 div ≈ 125 ex", muted when stale), populated by `loadExchange()` on load; hidden if served statically. Scope deliberately limited to Map Juicer — every other tool still uses poe.ninja `fetchCurrencyRates` (poe.ninja is free/unlimited; the official exchange spends the shared Trade2 budget). Verified live: returns exPerDiv 125 (30 offers), 2nd call served from cache with no trade call.
- 2026-06-13: Moved the Map Juicer paste evaluator to the top of `waystone-juicer.html`, directly below the page header and above the content tabs, so map/tablet evaluation is the first workflow on the page. Added bottom spacing to the evaluator block before the tabs.
- Verification (2026-06-13): Git status confirmed only `waystone-juicer.html` changed before the AGENTS.md handoff update. Full syntax/browser verification was not run because the Windows command runner intermittently failed to spawn commands (`CreateProcessAsUserW failed: 1312`) during this small HTML reorder.
- 2026-06-13: Added Arbitrage Scanner v1. New standalone `arbitrage-scanner.html`, hub nav/card links, `.arbitrage-scan-cache.json` gitignore entry, and `POST /api/arbitrage/scan` in `server.js`. Scanner uses the official Trade2 Currency Exchange through the shared `tradeQueue`, scans a curated high-volume currency/fragments universe against Exalted Orb, resolves current Trade2 static IDs when possible, and returns ranked flips by stock-capped net profit after slippage. UI is manual-refresh only with budget, min net profit, min ROI, min stock, slippage, and category toggles. Cache TTL is 2 minutes with stale fallback when Trade2 is limited.
- Verification (2026-06-13): `node --check server.js` passed. Inline-script/browser/API smoke checks were attempted but blocked by intermittent Windows sandbox process-spawn failures (`CreateProcessAsUserW failed: 1312`); no live Trade2 scan was run in this verification pass.
- Verification follow-up (2026-06-13): Continued Arbitrage Scanner testing. Re-read `arbitrage-scanner.html` and `server.js`; confirmed the endpoint is wired at `POST /api/arbitrage/scan`, uses the shared `tradeQueue`, and the UI posts the expected settings payload. `node --check server.js` passed again. Could not complete inline-script parse, local HTTP smoke, Playwright, or live Trade2 scan because the Windows command runner degraded to consistent process-spawn failures (`CreateProcessAsUserW failed: 1312`), including a trivial `Write-Output ok`.
- Verification follow-up (2026-06-13): Retried Arbitrage Scanner testing after the previous runner failure. Initial `Get-ChildItem` and `git status --short` succeeded and showed the expected dirty files (`.gitignore`, `AGENTS.md`, `index.html`, `server.js`, `waystone-juicer.html`) plus new `arbitrage-scanner.html`. Immediately after, `rg`, `Write-Output ok`, and `Get-Date` all failed before PowerShell could start with `CreateProcessAsUserW failed: 1312`, so no additional syntax/API/browser/live Trade2 verification could be completed in this pass.
- 2026-06-13: Resumed Arbitrage Scanner testing and fixed issues found during live verification. The running `server.js` process was stale and initially returned 404 for `POST /api/arbitrage/scan`; restarted it from this checkout. Live scan then exposed bad fallback exchange tags (`alchemy`, `annulment`, `artificers-orb`, `gemcutters-prism`) and an exchange response parser that did not traverse `result.<id>.listing.offers[]`. Fixed fallback IDs to official static tags (`alch`, `annul`, `artificers`, `gcp`), changed static lookup to the generic `/api/trade2/data/static`, taught `collectExchangeOffers` to traverse keyed result objects/offers, and made min-stock filtering apply to the non-exalted item side.
- Verification follow-up (2026-06-13): `node --check server.js` passed; `arbitrage-scanner.html` inline script parsed with Node `vm.Script`; server restarted from this checkout; `/arbitrage-scanner.html` served 200. A currency-only live scan (`currency:true, fragments:false`, min profit 1ex / ROI 1% / stock 10) reached real exchange IDs and parsed real offers, but stopped at the official exchange rate limit after several probes (`limited:true`, stale cache written, `tradeLimitedUntil` 2026-06-13T19:11:48Z). Remaining live result had no opportunities, `Regal Orb` `missing-side` (likely no qualifying stock side at min stock 10), then `Vaal Orb` `rate-limited`. Cached/limited API path returned immediately with stale data. Playwright opened `arbitrage-scanner.html`, confirmed controls render, clicked Scan, and verified the UI shows the Trade2 limited/stale state without script errors.
- 2026-06-13: Added the Trade2 limit timestamp to Arbitrage Scanner limited-state UI. The page now reads `tradeStatus.tradeLimitedUntil` (or top-level `tradeLimitedUntil`) and shows the local formatted time in both the status line and the empty limited-results message.
- Verification follow-up (2026-06-13): `node --check server.js` passed; `arbitrage-scanner.html` inline script parsed with Node `vm.Script`; `/api/trade-status` still showed `tradeLimitedUntil` during the active cooldown; Playwright clicked Scan and confirmed the UI rendered `Trade2 limited until 13/06/2026, 21:11:48...` plus the same timestamp in the empty-state message.
- 2026-06-13: Cross-page CONSISTENCY pass. Unified the nav across every page to one canonical set — Home · Craft Pricer · Rune Picker · Gear Search · Map Juicer · Arbitrage — with the current page marked `.active`. Fixes: waystone-juicer was missing Craft Pricer/Map Juicer/Arbitrage; arbitrage-scanner was missing Craft Pricer/Rune Picker/itself; character-upgrades was missing Map Juicer/Arbitrage. Added the missing `.navbtn.active` CSS to waystone-juicer + arbitrage-scanner, and a `.nav` flex-wrap. Standardised brand text to "PoE Tools" (was "PoE TOOLS"/"POE TOOLS") and `<title>` to "<Tool> — PoE Tools" (was "PoE2 Gear Search", "Arbitrage Scanner - PoE Tools", etc.). craft-optimizer.html was orphaned (no nav, brand→"/"); gave it the same nav + brand→index, kept it OUT of other pages' navs since it's experimental/unsurfaced. All 5 pages serve 200, inline scripts parse.
- 2026-06-13: Critically reviewed + fixed Codex's Arbitrage Scanner (root cause of the chronic rate-limiting). THE bug: the front-end `payload()` hardcoded `force:true`, so every Scan bypassed the 2-min cache and ran a full live sweep; combined with ~22 Trade2 calls per scan (2 exchange calls × 11 items) it tripped the shared limit constantly. Fixes: (1) `force` is now an opt-in "Force live refresh" checkbox (default off) — repeat scans return the cache in ~0.15s with ZERO trade calls. (2) Replaced per-item fetching with chunked batched exchange queries: `have`/`want` accept arrays, but the API rejects ≥~11 ("Too many items") AND a wide batch returns a capped page that starves currencies (a 9-want buy dropped Divine entirely). Verified a 3-wide batch covers every currency, so `fetchExchangeChunked` chunks the multi-currency side at `EXCHANGE_BATCH_CAP=3` and merges offer maps — ~8 calls per full scan vs 22, queue tied up ~25s vs ~165s. (3) Empty-state now says "Scanned N pairs — spread is usually negative for instant round-trips…" so a no-arbitrage result doesn't read as broken. Confirmed all 11 official static tags are valid (currency + both splinters); the earlier missing-items were coverage, not bad ids. Kept Codex's limit-timestamp UI.
- Verification (2026-06-13): `node --check server.js` + all 5 pages' inline scripts parse. Probed the exchange API directly: multi-want works in both `exchange:`/`query:` shapes, 11 wants → HTTP 400 "Too many items", 3 wants → full coverage. Forced scan (cap 3, both categories): universe 11, no rate-limit, ~8 calls (~42s), 9/11 covered (Regal + Simulacrum Splinter thin/page-starved, low liquidity), 0 profitable opportunities = correct (Exchange spread is negative for instant flips). Non-forced scan returned `cached:true` in 0.15s with no trade calls. Was deliberately conservative with the shared limit throughout (checked status before every probe, spaced via the queue) — never tripped it.
- 2026-06-13: Premium dark REDESIGN + shared design system. Created `theme.css` (linked first on all 5 pages) as the single token source — warm near-black surfaces, restrained gold, brighter muted text (the main fix for the "muddy" low-contrast feel), Cinzel/Inter/JetBrains type via @import, global niceties (custom scrollbars, focus ring, ::selection, gold checkbox accent-color, tabular-nums), and tasteful motion (card entrance fadeUp + stagger, hover lift/amber-glow) gated behind `prefers-reduced-motion`. Each page now links theme.css and DROPS its own `:root` so it inherits the unified palette; the standalone pages previously loaded NO web fonts (system fallback) + had divergent palettes, which is why they looked flat/unfinished next to the hub. craft-optimizer's hex/alt-named tokens (`--gold/--text/--green/--mono/--display`) resolve via aliases added to theme.css. Hub got a signature gold tooltip-rule on live cards + smoother hover. Unified brand wordmark + page h1 to Cinzel on Map Juicer/Arbitrage. CSS-ONLY — no markup/ID/localStorage/JS/route changes, so all tool logic is preserved.
- Verification (2026-06-13): Playwright (chromium-1223 via npx-cached playwright 1.60, NODE_PATH to the npx module dir) screenshotted all 5 pages at 1366×900 — 0 console/page errors on every page; fonts load (Cinzel h1/brand). CSS-var audit (node): every `var(--x)` on every page resolves against theme.css or the page's own styles (no undefined tokens from the :root removal). Interaction test (no trade calls): hub hash-routing switches the Rune Picker view + highlights nav, cross-page nav loads Map Juicer (6 tabs), Map Juicer JS tab-switch works, Arbitrage Scan button + Force-refresh checkbox present. theme.css serves 200 text/css. Did NOT use web-artifacts-builder (React/shadcn) — would violate the zero-dep/no-framework constraint; used vanilla CSS + frontend-design/theme-factory guidance + webapp-testing instead.
- 2026-06-13: Made the whole site responsive (desktop/tablet/mobile). Most of it landed in `theme.css` so it covers all 5 pages at once: a shared responsive layer scoped under `.appbar`/`.bar` (higher specificity than a bare `.nav`, so it overrides per-page rules despite theme.css loading first). On ≤820px the app bar becomes a flex row that wraps — brand/wordmark stay inline, and the nav turns into a tidy single horizontally-scrollable row (hidden scrollbar + right-edge fade mask hinting more), replacing the old wrap-and-stretch (`.navbtn{flex:1}`) mess. Also added `pointer:coarse` min touch-target sizes and `img/svg/video{max-width:100%}`. Per-page: craft-optimizer was the only document-level overflow (its 980px-min table) — fixed with `.grid>*{min-width:0}` so the `.tablewrap` scrolls internally instead of pushing the page, plus full-width controls and tighter ≤560 padding. Every other page already collapsed via existing media queries; the shared nav rule was the only fix they needed. CSS-only.
- Verification (2026-06-13): Playwright at tablet (768) + mobile (390) across all 5 pages — horizontal page overflow is 0px everywhere (craft-optimizer was 266px before), 0 console/page errors. Eyeballed hub/gear/arbitrage/map-juicer/craft-optimizer at mobile: nav scroll-row + inline brand, grids collapse to 1 col, controls stack full-width, Map Juicer tabs wrap, tables scroll inside their wrappers. Desktop (1366) regression check: 0 overflow, nav still uses normal wrap (≤820 rules correctly don't apply).
- 2026-06-14: REBUILT the visual system after the v1 redesign was rejected as "old, muddy, flat, boxy". Root cause of v1: it reused the warm-brown oklch palette (the muddiness), kept sharp bordered text-boxes, and put Cinzel+gold on everything. theme.css v2 = neutral charcoal surface scale (near-black, ~0 chroma, no brown), Inter for all UI + Cinzel ONLY for the brand mark, gold as a sparse accent, light-alpha hairline borders, real radii (8–20px) + soft layered shadows + focus rings, and reusable components (`.btn`/`.badge`/`.card`/`.field`). Rebuilt the hub (index.html) into a real dashboard: blurred sticky header w/ active-nav underline indicator, hero with eyebrow + big Inter headline + stat tiles, and tools as modern PRODUCT cards (inline-SVG icon tile → live badge → name → description → action button) replacing the flat bordered grid. Propagated to all tool pages: shared modern header/nav via theme.css (scoped `.appbar`/`.bar`), rounded panels/inputs/tabs (sharp 2px → token radii), gradient surfaces + soft shadows, filled-gold primary buttons. Caught+fixed a self-referential CSS-var cycle (`--gold:var(--gold)` etc.) that was blanking tokens to black. Preserved all routes/IDs/data-view-link/localStorage/JS — CSS + hub-markup only.
- Verification (2026-06-14): Playwright across all 5 pages at desktop/tablet(768)/mobile(390): 0 console errors, 0 horizontal overflow everywhere. Computed-style check confirmed tokens resolve (`--gold`=oklch(82% .135 78), `--text`=oklch(97%…)) and h1 colors are correct (white Inter on hub, gold Cinzel on tool pages) after the cycle fix. Visually reviewed hub + rune-picker + map + arbitrage + gear + craft-optimizer: cohesive charcoal dashboard, modern nav w/ active indicator, product cards, rounded panels. Routing/tab/control interactions still work (no trade calls).
- 2026-06-14: MERGED all 3 standalone tools into the SPA shell (true inline views, no iframes) + polished tables. Tables: shared `.tablewrap` styling in theme.css (zebra, row-hover, sticky header, mono numerics). Shell merge: Arbitrage → inline (`@scope`, `__viewInit` lazy fn, IDs renamed `#ab*`); Map Juicer → inline (`@scope`, lazy fn, `waystone-data.js` loaded by shell); Gear Search → inline (CSS `@scope`, JS extracted to `gear-search.js` lazy-wrapped, 1020 lines). Standalone pages → redirects. Removed the interim iframe approach + its CSS. See `## Tools — status` for the architecture.
- Verification (2026-06-14): Playwright full walk — all 7 views render inline (0 iframes), `active`/routing correct, 0 horizontal overflow at desktop+mobile, **0 console/page errors across the entire walk**. Interactions (no Trade2 calls): Arbitrage checkbox; Map Juicer tab-switch + evaluate (rarity-72 → "Premium juice, Item Rarity 72% ≈ 356ex, pair Expedition"); Gear Search add-filter + query-preview render + slot wizard. Confirmed gear init hits only local endpoints (`/api/trade-status`, `/api/gear-search/analyze`) — 0 Trade2 on open. Redirects resolve (`character-upgrades.html`→`index.html#gear-search` etc.); deep-link `index.html#gear-search` loads inline. `@scope` isolation verified (e.g. inline arb `.panel` radius = 10px, no bleed). gear-search.js + all index inline scripts parse.
- 2026-06-14: HARDENED the SPA merge (post-review). (1) Removed the `@scope` browser dependency — converted each tool's `@scope(.toolroot-x)` block to explicit `.toolroot-x <sel>` rules (script `_unscope.js`, since deleted): higher specificity beats theme.css like @scope's proximity, but works in every browser. Verified no bleed (home `.tool` still 14px) and tools render identically. (2) Fixed a real DEEP-LINK bug: `showView()` ran in the main script before the later tool `<script>`s registered their `__viewInit`, so direct loads to `index.html#<tool>` (and the redirect targets) left the tool uninitialised — added `DOMContentLoaded → runViewInit(currentView())`. (3) Fixed Gear Search mobile overflow (its 2-col `main` never collapsed + the queue-status pushed off-screen): collapse `main`→1col and stack `.pagehead` below 980px. (4) Gear Search status interval now only polls while its view is visible (no background leak). (5) a11y: focus the new view's heading + `aria-live` announce on nav (sr-only `#view-live`). (6) Replaced Gear Search's leftover brown oklch surfaces with charcoal tokens.
- Verification (2026-06-14): Ran ONE real inline Arbitrage scan (justified, deliberate; queue self-throttled to 30s gap but never `limited`) — completed end-to-end (cache: universe 11, 0 opps = correct), proving the inline Trade2 flow works. Map Juicer's div↔ex chip + Gear Search's `/api/gear-search/analyze` also confirmed working inline. Final regression walk (all views, desktop+mobile): 0 iframes, 0 horizontal overflow, **0 console/page errors**. Deep-link inits verified (#map-juicer tabs+patch, #gear-search preview). Known remaining: arbitrage result TABLE styling still unproven with rows (the exchange has no actual arbitrage, so it always renders the empty state — can't force a table); index.html is a ~2000-line monolith (gear JS externalised to gear-search.js, arb/map still inline — a deliberate maintainability tradeoff, not a defect).
- 2026-06-14: Added `smoke-test.js` (see Conventions) + verified the arbitrage result TABLE styling by mocking row data in the browser check (sticky header + zebra confirmed — it can't be hit live since the exchange has no real arbitrage). Negative-control: removing the deep-link `DOMContentLoaded` fix makes the test fail 5 checks; restored → 28/28.
- 2026-06-14: SPLIT the monolith. Each inlined tool is now its own files: `arbitrage.css`+`arbitrage.js`, `map-juicer.css`+`map-juicer.js`, `gear-search.css`+`gear-search.js` (`waystone-data.js` is map's data dep). index.html dropped 1611→995 lines = shell markup + the main app `<script>` + `<link>`/`<script src>` for the tools. Zero-risk as predicted (CSS already prefix-scoped, JS already lazy `__viewInit` fns); smoke test 37/37 after the move. To add a tool: a `.css`, a `.js` (wrap in `window.__viewInit["<view>"]=function(){…}`), a `<section class="view toolroot-<x>">` in index, a nav link, and the two `<link>`/`<script src>` tags.
- HOW TO FIX THE MONOLITH (DONE 2026-06-14 — see above; kept for reference) — was: index.html ~2000 lines — recommended, zero-risk because tool CSS is already prefix-scoped (`.toolroot-x`) and tool JS is already isolated lazy fns: (1) Move each tool's `.toolroot-x …` CSS block out of index's `<style>` into its own file (`arbitrage.css`/`map-juicer.css`/`gear-search.css`, or one `tools.css`) and `<link>` it after theme.css — cascade is unchanged (specificity, not order, is what makes `.toolroot-x` win). (2) Move Arbitrage's + Map Juicer's `window.__viewInit["x"]=function(){…}` into `arbitrage.js`/`map-juicer.js` and `<script src>` them before `</body>`, exactly like the existing `gear-search.js` — the registry + DOMContentLoaded init already handle them regardless of file. (3) KEEP the view markup inline (HTML can't be linked without a build step or a fetch-include + flash; markup is the small part). Result: index ≈ shell markup + main app script (~700 lines); each tool = 1 css + 1 js + a `<section>`. Run `node smoke-test.js` after to confirm nothing moved wrong. (NOT a build step — preserves the zero-build edit-and-refresh workflow.)
- 2026-06-14: FINISHED the modularization — extracted Rune Picker → `rune-picker.js` and Craft Pricer → `craft-pricer.js` (its CRAFTS data preserved for the rebuild) out of index's main script. index.html 995→569 lines = pure shell (routing + markup). The shell's old per-view hooks (`if(key==="rune-picker") refreshTradeStatus()`, craft auto-refresh) are gone — each tool's `__viewInit` does its own setup on first open. Tool JS is now fully modularised (arb/map/gear/rune/craft each have a `<tool>.js`). CSS is NOT fully symmetric yet: arb/map/gear have their own prefix-scoped `<tool>.css`, but **Rune Picker's + Craft Pricer's CSS still lives in index's inline `<style>` (~125 lines)** — it's intertwined with shared page-head/table/input rules that the placeholder/coming-soon views also use, so it wasn't cleanly separable. (Follow-up: wrap the rune-picker view in `.toolroot-rune` and lift its rune-only rules into `rune-picker.css`.) Extended smoke test to 43 checks (incl. rune-picker/craft-pricer parse+serve, "index is shell-only", and a Rune Picker wiring check: empty Check-picks → guard message). Verified: 43/43, Rune Picker renders + wired identically (visual), 0 errors.
- 2026-06-14: CSS PARITY (the follow-up above, now done). Wrapped the rune-picker view in `.toolroot-rune` and lifted its rune-only rules (`.toolpanel/.toolbar/.toolbody/.tooltitle`, `.paste-zone*`, `#pastePreview`, `.split`, `.bestbox*`, `.conf*`, `.sample`, `#checkRunes`) into `rune-picker.css`, prefix-scoped. Dropped dead `#refresh` rule (0 uses). Verified which selectors were truly rune-only vs shared before moving: `.muted` (gear uses 9×), `textarea` (gear/map), `.controls` (arb), `.status` (gear), `.pagehead/.sub` (6 views), `.tablewrap/table` (rune+theme; arb overrides) are SHARED → left as the inline baseline. So index `<style>` now = home/hub + shared baseline only, no tool-specific CSS. Smoke test → 47 checks (added: rune-picker.css linked+served, index `<style>` purged of rune-only selectors, rune-picker.css holds them prefix-scoped, and a live-browser check that `.toolroot-rune .toolpanel` is actually painted). 47/47, rune `.split` confirmed 2-col on desktop / 1-col mobile, screenshot pixel-identical.
- 2026-06-14: Four-part update. (1) **Craft Pricer rebuilt** — restored its markup (`.toolroot-cp`: pagehead controls + `#cpGrid`) and added `craft-pricer.css`; the JS was already complete (queries `/api/prices` → poe.ninja, renders per-route material rows + required-floor/with-optionals totals in chaos & divine). IDs namespaced `#cp*` to dodge gear-search's `#status`. (2) **Home currency strip** (`home.js` + `.fxstrip` CSS in index, under the hero) — reads the new cached `GET /api/currency/overview` (poe.ninja via `fetchCurrencyRates`, file-cached `.currency-overview.json`, 10-min TTL, `?refresh=1` forces; poe.ninja NOT Trade2 so the ↻ button is rate-limit-safe). Shows Divine/Exalted/Chaos/Annul/Regal/Vaal/Chance/Alch in ex. (3) **Map Juicer regex FIXED & made %-aware** — the old presence-substrings (`"ack s"`, pipes mis-scoped, `!` outside quotes) were wrong. PoE2 0.5 waystones show a compact "Label: +X%" reward block; correct tokens bridge the label to its colon and append a value range, e.g. `"i.+ty: \+([6-9].|1..)%"` = Item Rarity ≥60% (negation is `"!…"` INSIDE quotes). Tokens now in `waystone-data.js` `tokens.line` + `tokens.revivesZero`; map-juicer builds %-aware run/blue regex with adjustable Min-Rarity/Min-Pack selects. Source: official forum regex guide view-thread/3858429 (+ VULKK, poe2.re). (4) **0-revives regex added** — `"revives available: 0"` finds fully-juiced 6-mod maps (revives go 6→0 as mods are added; it's a property line, not a rolled mod). Smoke test 47→**63** (currency endpoint+strip+refresh, craft-pricer grid renders 6 cards, %-aware regex range + 0-revives + threshold-select rebuild, home/craft mobile overflow). 63/63; all three surfaces screenshot-verified with live data.
- 2026-06-14: **Unified all currency pricing onto the Trade2 Currency Exchange (poe.ninja dropped for currency).** There is ONE shared Trade2 queue (`trade-queue.js` → `tradeQueue`); everything Trade2 already went through it. The fix was the *data source*: added `getExchangeData(league,force)` / `getExchangeRates(league)` in `server.js` — derives ex-values for the 9 main currencies from the live Trade2 exchange (reuses `fetchExchangeChunked` + `bestExchangeOffer`, one batched ex→currency call ≈3 chunked requests), file-cached `.currency-rates.json` (10-min TTL, serves cache when rate-limited, `?refresh=1` forces). `rates` is keyed by currency id + normalised name (drop-in for the old poe.ninja `fetchCurrencyRates`, which is now DEAD CODE — left defined, unused). Repointed every consumer: home strip, Gear Search price conversion (×3 sites), Rune Picker, Upgrades. **Rune Picker** now drops the poe.ninja "Currency" category and prices currency rewards off the exchange catalog (source label `trade2 exchange`); non-currency catalogs (runes/essences/soul cores/gems) still come from poe.ninja. **Currency icons**: `resolveArbitrageItems` now also captures `iconsById` from the Trade2 `data/static` endpoint (`normalizeIconUrl` → absolute pathofexile.com URLs); the home strip renders them. Robustness: rate uses the best offer with **stock≥5** (fallback to any) so one thin lowball listing can't skew it — this corrected Annul from a thin-offer 10ex to a real ~69ex. Live-verified: home strip (10 real icons, Divine 150ex), Rune Picker currency rows (`trade2 exchange`). Smoke 63→**67** (unified-provider + no-fetchCurrencyRates + iconsById static checks; currency chips render `img.fxicon`). NOTE: the `/api/currency/overview` httpCheck hits the real endpoint, but it's cache-gated (10-min) + queue-throttled, so it makes at most ~4 throttled Trade2 calls per test run on a cold cache — not exhausting. Supersedes the previous entry's "home strip uses poe.ninja".
- 2026-06-14: **Widened the currency-rate list + deleted dead `fetchCurrencyRates`** (the two follow-ups from the Trade2-currency-unification session). (1) Added the common exotic gear-price orbs — Mirror of Kalandra, Orb of Transmutation, Orb of Augmentation, Fracturing Orb — to `ARBITRAGE_ITEMS` as `category:"currency", enabled:false`. The `enabled:false` flag keeps them OUT of the arbitrage scanner (the scan filters on `item.enabled`, server.js ~575) while `fetchExchangeData`'s currency filter (~1036, no `enabled` check) still derives ex-rates for them, so Gear Search listings priced in these no longer get dropped for lack of a rate (poe.ninja used to cover all currencies; the Trade2 move had narrowed it to the 9 scanner currencies). Resolution via the static endpoint sets each id, so the fallback ids are just backstops. (2) Deleted the unused poe.ninja `fetchCurrencyRates` (was ~954); its only helper deps (`getDisplayPriceExalted`, `normalizeName`, `NINJA_TIMEOUT_MS`) are still used elsewhere so nothing else was orphaned. Append more rate-only currencies in the commented block after `gcp` as needed (each adds a fraction of a chunked exchange call; still cache-gated 10-min).
- Verification (2026-06-14): `node --check server.js` passed; `node smoke-test.js` → **67/67**. The smoke run's `/api/currency/overview` fetch (cold cache) repopulated `.currency-rates.json`: transmute (0.5ex), aug (0.14ex), fracturing-orb (1199ex) resolved with real rates; mirror has no exalted→mirror exchange offer (mirrors trade in divine, not exalted bulk) so it's skipped gracefully — an inherent liquidity gap, not a bug. Kept within the shared Trade2 budget (one cache-gated, queue-throttled refresh).
- 2026-06-14: Home currency strip formatting polish (`home.js`). (1) `fmtEx` now caps at **2 decimals** everywhere (≥100 whole, ≥10 → 1dp, else 2dp) — was showing 3dp for sub-1 values (e.g. aug `0.1429`→`0.14`). (2) Any currency worth **more than 1 Divine** in exalts now renders its value in **div** instead of a large ex number (e.g. Fracturing Orb 1199ex → `7.99 div`); Divine itself and sub-divine currencies stay in ex. Verified against the live cache + `node smoke-test.js` 67/67.
- 2026-06-14: Hub hero now shows a **live economy stat** (the AGENTS "Next" item). The static "Ready / Economy data" tile (`#homePriceStatus`) is repopulated by `home.js` `render()` with the live Divine price (`fmtEx(divineEx) + " ex"`, label "per Divine", "· stale" when the cache is rate-limited). Reuses the `/api/currency/overview` data the strip already fetches — no extra request. Smoke test +1 (mocked divine ex=320 → hero reads "320 ex"), now **68/68**.
- 2026-06-14: Arbitrage Scanner now shows **near-miss spreads** (the "show it scanned even when 0 profitable" follow-up). `scanArbitrage` records EVERY evaluated pair (not just qualifying ones) and returns `nearMiss` = the top 3 by net profit that didn't clear the thresholds. `arbitrage.js` renders them as a compact "Closest spreads found" table inside the empty state, so the common 0-opportunity result (round-trip Exchange spreads are usually negative) reads as a real scan with honest numbers rather than a broken/empty box. CSS `.nearmiss`/`.nearmiss-head` in arbitrage.css. Smoke +1 (mocked empty result + 2 nearMiss rows → table renders), **69/69**. Did NOT run a live scan (rate-limit caution); logic verified via mock.
- 2026-06-14: **Self-hosted the 3 fonts** (the #1 redesign follow-up) — true offline, no Google Fonts dependency. theme.css `@import` (Google CSS2) → three `@font-face` rules pointing at `/fonts/*.woff2`. All three families ship as a single VARIABLE woff2 each (latin subset; Google serves one file per family covering the weight range), declared with weight ranges: Inter `400 700`, Cinzel `600 700`, JetBrains Mono `400 600` (`inter.woff2` 48KB, `cinzel.woff2` 26KB, `jetbrains-mono.woff2` 31KB — ~105KB total, committed to `fonts/`). Added `.woff2 → font/woff2` to the server MIME map (was octet-stream fallback). Font stacks already fall back to system fonts if a file is missing. Smoke +5 (theme self-hosts / no Google @import / references all 3 / each serves 200 font/woff2), **74/74**. To refresh a family: re-fetch its variable woff2 from Google CSS2 (browser UA, take the `/* latin */` block's url) into `fonts/`.
- 2026-06-14: **Denser data-table polish.** The shared `.tablewrap` zebra was `oklch(.018)` — effectively invisible; bumped to `.032` (perceptible but still subtle), tightened cell padding (`9/10px → 8/9px` vertical for density), added a header bottom-inset hairline and a gold left-accent bar (`inset 2px 0 0 var(--gold)`) on row hover. Also gave the gear-search `.projection-table` + `.comparison` tables (which had NO zebra/hover) the same zebra + gold-soft row-hover for parity. theme.css (shared, covers rune + arbitrage) + gear-search.css. Verified: arbitrage table zebra/sticky checks still pass, 0 overflow regression, **74/74**. Honest note: the shared changes are guarded by the mocked-row arbitrage check; the gear-table additions are additive CSS (tokens resolve, thead/tbody confirmed) but not separately screenshot-verified live.
- 2026-06-14: **Adaptive backfill for exchange page-starvation** (`fetchExchangeChunked`, server.js). A high-liquidity currency (Divine) in a 3-wide chunk can fill the single capped response page and starve its chunk-mates of offers. After the batched pass, the fn now re-fetches any item that came back with ZERO offers, ALONE (its own page), bounded by `EXCHANGE_BACKFILL_CAP=6` and skipped entirely on an empty merged (rate-limit/systemic failure, not starvation) so it can't burst. Benefits both the arbitrage scan and currency-rate sweep. **Made server.js requireable** for tests: `server.listen` is now guarded by `require.main === module`, and it exports `{fetchExchangeChunked, collectExchangeOffers, bestExchangeOffer, __setExchangeRawImpl}` (the latter injects a stubbed network). New `backfill-test.js` exercises the REAL code with a stubbed whale-starvation page (9 assertions: every starved item recovered, whale not re-fetched, zero waste when nothing starved) — wired into smoke as a static check, **75/75**. Verified live (careful, spaced, queue self-throttled, never tripped the limit): a forced scan ran clean (HTTP 200, not limited); at `minStock:1` coverage is 9/11 with only Regal Orb + Simulacrum Splinter missing — and they're missing at presence-level too, i.e. today they genuinely have NO exalted-pair offer one side (a real market gap, NOT page starvation the backfill can fix). So the fix is a correctness/robustness improvement proven by the offline determinism test; it can't manufacture offers that don't exist.
- Next: Redesign follow-ups: (fonts self-hosted ✓, hub hero live stat ✓, table polish ✓ — done). Arbitrage follow-ups: page-starvation backfill now in place ✓ (remaining Regal/Simulacrum missing-side is genuine market absence of exalted pairs, not starvation); near-miss spreads now shown ✓; defaults (5ex/3%/stock5) will usually show empty since real arbitrage is rare. Map Juicer follow-ups: regex tokens now follow the official 0.5 "Label: +X%" format (forum guide) but were NOT verified in a live stash — confirm wording in-game and adjust `waystone-data.js` `tokens.line` if a label differs; re-run the price-floor sweep each patch; optional currency-cost of juice path via poe.ninja; add Runes of Aldur league mechanic as its own content type if it has dedicated tablets. Also continue Gear Search hardening (more imported item shapes / edge-case exports).

